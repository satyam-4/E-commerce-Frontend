import { Skeleton } from "@/components/ui/skeleton";

export default function ProductSidebarSkeleton() {
    return (
        <Skeleton className="min-w-[300px] min-h-[1400px] bg-zinc-200">
        </Skeleton>
    )
}